# .env.example â€” copy to .env and fill in real values. NEVER commit .env.
# Keep this file in sync: every var the project reads should appear here as a placeholder.

APP_ENV=development
# API_KEY=
# DATABASE_URL=
